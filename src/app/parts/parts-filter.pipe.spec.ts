import { PartsFilterPipe } from './parts-filter.pipe';

describe('PartsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PartsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
